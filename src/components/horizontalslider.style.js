import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  films: {
    height: 260
  },
  titleFilms: {
    fontSize: 25,
    fontWeight: 'bold',
    margin: 10
  }
})

export default styles